---
"@tinycloud/cli": patch
---

Honor Share subcommands' `--json` flag when the root CLI also defines that option, restoring machine-readable success output for inspect, receive, and lifecycle commands.
