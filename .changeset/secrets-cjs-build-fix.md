---
"@tinycloud/secrets": patch
---

Fix TinyCloudWeb constructor config (tinycloudHosts, providers.web3.driver), switch to static web-sdk import, add ssr=false layout, parallelize vault/kv fetches, and add Playwright smoke tests for CJS bundling regression.
